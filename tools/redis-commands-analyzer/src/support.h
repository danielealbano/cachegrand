#ifndef ANALYZER_SUPPORT_H
#define ANALYZER_SUPPORT_H

char* replace_char(
        char* str,
        char find,
        char replace);

char* read_file(
        char *filename);

#endif //ANALYZER_SUPPORT_H
